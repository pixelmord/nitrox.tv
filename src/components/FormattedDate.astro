---
interface Props {
	date: Date;
	class?: string
}

const { date, ...rest } = Astro.props;
---

<time datetime={date.toISOString()} {...rest}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
